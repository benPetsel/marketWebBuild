events {
    # how many connections we allow
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    # Include the server config for your application
    include /etc/nginx/app.conf;
}